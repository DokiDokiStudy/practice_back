---
title: "1.3 실습 환경 구축하기"
categoryPath:
  - "Docker"
  - "1장 시작하기 전에"
  - "1.3 실습 환경 구축하기"
authorNick: "관리자"
updateMode: "upsert"
---

## 시스템 요구사항

### 최소 요구사항
- **메모리**: 4GB RAM 이상 (8GB 권장)
- **디스크**: 20GB 이상의 여유 공간
- **운영체제**: 
  - Windows 10/11 (Pro, Enterprise, Education) 또는 Windows 10 Home (WSL2 사용)
  - macOS 10.14 (Mojave) 이상
  - Linux 커널 3.10 이상 (Ubuntu 18.04+, CentOS 7+, Debian 9+ 등)

### 권장 요구사항
- **메모리**: 16GB RAM 이상
- **디스크**: SSD 50GB 이상
- **CPU**: 가상화 지원 (Intel VT-x, AMD-V)

## Docker 설치하기

### Windows에서 Docker 설치

#### 방법 1: Docker Desktop (권장)
```powershell
# 1. Docker Desktop 다운로드
# https://desktop.docker.com/win/stable/Docker%20Desktop%20Installer.exe

# 2. 설치 후 재시작

# 3. WSL2 Backend 활성화 (Windows 10 Home인 경우 필수)
```

#### 방법 2: WSL2 + Docker Engine
```bash
# WSL2에서 Ubuntu 사용 시
sudo apt update
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Docker 공식 GPG 키 추가
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Docker 저장소 추가
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Docker Engine 설치
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io

# 사용자를 docker 그룹에 추가
sudo usermod -aG docker $USER
newgrp docker
```

### macOS에서 Docker 설치

#### Intel Mac
```bash
# Docker Desktop for Mac 다운로드 및 설치
# https://desktop.docker.com/mac/stable/Docker.dmg

# Homebrew 사용 시
brew install --cask docker
```

#### Apple Silicon Mac (M1/M2)
```bash
# Apple Silicon용 Docker Desktop 다운로드
# https://desktop.docker.com/mac/stable/amd64/Docker.dmg

# Rosetta 2 설치 (필요한 경우)
softwareupdate --install-rosetta
```

### Linux에서 Docker 설치

#### Ubuntu/Debian
```bash
# 기존 Docker 패키지 제거
sudo apt remove docker docker-engine docker.io containerd runc

# 필수 패키지 설치
sudo apt update
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Docker 공식 GPG 키 추가
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Docker 저장소 추가
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Docker Engine 설치
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io

# Docker 서비스 시작 및 자동 시작 설정
sudo systemctl start docker
sudo systemctl enable docker

# 현재 사용자를 docker 그룹에 추가
sudo usermod -aG docker $USER
```

#### CentOS/RHEL/Fedora
```bash
# 기존 Docker 패키지 제거
sudo yum remove docker docker-client docker-client-latest docker-common docker-latest docker-latest-logrotate docker-logrotate docker-engine

# yum-utils 설치
sudo yum install -y yum-utils

# Docker 저장소 추가
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# Docker Engine 설치
sudo yum install -y docker-ce docker-ce-cli containerd.io

# Docker 서비스 시작
sudo systemctl start docker
sudo systemctl enable docker

# 현재 사용자를 docker 그룹에 추가
sudo usermod -aG docker $USER
```

## 설치 확인

### 기본 명령어로 확인
```bash
# Docker 버전 확인
docker --version
# 출력 예시: Docker version 24.0.2, build cb74dfc

# Docker 시스템 정보 확인
docker info

# Docker 데몬 상태 확인
docker version
```

### Hello World 컨테이너 실행
```bash
# Docker가 정상 작동하는지 확인
docker run hello-world

# 성공 시 출력되는 메시지:
# Hello from Docker!
# This message shows that your installation appears to be working correctly.
```

## 추가 도구 설치

### Docker Compose 설치

#### Windows/macOS (Docker Desktop 포함)
Docker Desktop을 설치했다면 Docker Compose가 자동으로 포함됩니다.

#### Linux
```bash
# 최신 버전 다운로드 (버전은 상황에 따라 변경)
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 실행 권한 부여
sudo chmod +x /usr/local/bin/docker-compose

# 버전 확인
docker-compose --version
```

### VS Code 확장 프로그램 (선택사항)
```bash
# VS Code Extensions
# - Docker (Microsoft): 컨테이너 관리 및 Dockerfile 편집
# - Remote-Containers: 컨테이너 내에서 개발
# - YAML: docker-compose.yml 파일 편집 지원
```

## 개발 환경 설정

### 작업 디렉토리 생성
```bash
# 실습용 디렉토리 생성
mkdir ~/docker-practice
cd ~/docker-practice

# Git 저장소 초기화 (선택사항)
git init
```

### 에디터 설정
- **VS Code**: Docker 확장 프로그램 설치
- **IntelliJ IDEA**: Docker 플러그인 활성화
- **Vim/Neovim**: Dockerfile 문법 하이라이팅 플러그인

## 네트워크 및 방화벽 설정

### 포트 확인
```bash
# Docker 데몬이 사용하는 포트 확인
netstat -tlnp | grep :2375  # Docker API (보안상 비활성화 권장)
netstat -tlnp | grep :2376  # Docker API (TLS)
```

### 방화벽 설정 (필요한 경우)
```bash
# Ubuntu/Debian - UFW 사용 시
sudo ufw allow 2376/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# CentOS/RHEL - firewalld 사용 시
sudo firewall-cmd --permanent --add-port=2376/tcp
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload
```

## 트러블슈팅

### 일반적인 문제들

#### 권한 오류
```bash
# "permission denied" 오류 시
sudo usermod -aG docker $USER
newgrp docker

# 또는 임시로 sudo 사용
sudo docker run hello-world
```

#### Docker 데몬 미실행
```bash
# Linux에서 Docker 서비스 시작
sudo systemctl start docker

# Windows/macOS에서 Docker Desktop 재시작
```

#### 디스크 공간 부족
```bash
# 미사용 컨테이너, 이미지, 네트워크 정리
docker system prune -a

# 볼륨까지 포함하여 정리
docker system prune -a --volumes
```

### 성능 최적화

#### Docker Desktop 설정 (Windows/macOS)
- **Memory**: 8GB 이상 할당 (시스템 메모리의 50-70%)
- **CPU**: 4 코어 이상 할당
- **Disk Image Size**: 100GB 이상 설정

#### WSL2 최적화 (Windows)
```bash
# .wslconfig 파일 생성 (C:\Users\<username>\.wslconfig)
[wsl2]
memory=8GB
processors=4
swap=2GB
```

이제 실습 환경이 준비되었습니다! 다음 단계에서는 Docker의 기본 사용법을 실습해보겠습니다.